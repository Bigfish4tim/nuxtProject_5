<template>
    <div>
        <v-row>
            <v-col>
                <v-btn>새로고침</v-btn>
            </v-col>
            <v-col>
                <v-btn>2022년도 대체공휴일 적용하기</v-btn>
            </v-col>
            <v-col>
                <v-btn>2023년도 대체공휴일 적용하기</v-btn>
            </v-col>
        </v-row>
        <v-data-table
            :headers="headers"
            :items="items"
            :search="allFilterTextSearchClone"
            hide-default-header
            :items-per-page="100"
            :footer-props="{
                'items-per-page-options': [10, 50, 100]
            }"
        >
            <template v-slot:body.prepend="headers">
                <tr class="topbody">
                    <td
                        v-for="(header, i) in headers.headers"
                        :key="i"
                        class="topbody_data"
                        style="text-align: center;"
                    >
                        {{ header.text }}
                    </td>
                </tr>
            </template>
        </v-data-table>
    </div>
</template>
<script>
import AccidentAreaFilters from "../../../mixins.js/Manager/diseaseAdminCode/AccidentArea/AccidentAreaFilters"
import AccidentAreaList from "../../../mixins.js/Manager/diseaseAdminCode/AccidentArea/AccidentAreaList"
import Resizable from "../../../mixins.js/Resizable"
import ExcelDownloader from "../../../mixins.js/ExcelDownloader"

export default {
    mixins: [
        Resizable,
        ExcelDownloader,
        AccidentAreaFilters,
        AccidentAreaList,
    ],
    data() {
        return {
            filterMenu: false,

            items: [],
        }
    },
    computed: {
        headers() {
            return [
                {
                    text: '기능',
                    align: 'center',
                    value: 'function',
                    width: '140px',
                },
                {
                    text: '시도',
                    align: 'center',
                    value: 'accidentSido',
                    width: '140px',
                },
                {
                    text: '구군',
                    align: 'center',
                    value: 'accidentgugun',
                    width: '140px',
                },
            ]
        },
        filterdateRange () {
            console.log(this.filterDate)
            return this.filterDate.join(' ~ ')
        },
    },
    methods: {
        sumReducer(prev, curr) {

            if(prev === '') {
                var intprev = 0
            } else {
                var intprev = parseInt(prev.replace(/,/g , ''))
            }

            if(curr === '') {
                var intcurr = 0
            } else {
                var intcurr = parseInt(curr.replace(/,/g , ''))
            }
            var sum = intprev + intcurr
            
            return sum.toLocaleString('ko-KR')
        },
    },
}
</script>
<style>
    
</style>